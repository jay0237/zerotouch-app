apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zerotouch.fullname" . }}-test-connection"
  labels:
    {{- include "zerotouch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zerotouch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
