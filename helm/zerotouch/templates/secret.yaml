apiVersion: v1
kind: Secret

metadata:
  name: zerotouch-secret
  namespace: zerotouch

type: Opaque


stringData:
  DB_USER: {{ .Values.secret.DB_USER }}
  DB_PASSWORD: {{ .Values.secret.DB_PASSWORD }}

  
