apiVersion: v1
kind: Service
metadata:
  name: {{ include "zerotouch.fullname" . }}
  labels:
    {{- include "zerotouch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zerotouch.selectorLabels" . | nindent 4 }}
